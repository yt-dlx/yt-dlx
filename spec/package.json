{
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "aio": "yarn build && node build/aio.js",
    "start": "yarn build && node build/index.js"
  },
  "dependencies": {
    "colors": "^1.4.0",
    "express": "^5.1.0",
    "typescript": "^5.8.3",
    "yt-dlx": "^14.0.10",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/express": "^5.0.1"
  }
}
