import dotenv from "dotenv";
import colors from "colors";
import YouTubeDLX from "..";

dotenv.config();
console.clear();

YouTubeDLX.Info.Extract({ query: "Soniye" })
  .on("data", data => console.log(data))
  .on("error", error => console.error(error));
YouTubeDLX.Info.Extract({ query: "Soniye" })
  .on("data", data => console.log(data))
  .on("error", error => console.error(error));
YouTubeDLX.Info.Extract({ query: "Soniye" })
  .on("data", data => console.log(data))
  .on("error", error => console.error(error));
YouTubeDLX.Info.Extract({ query: "Soniye" })
  .on("data", data => console.log(data))
  .on("error", error => console.error(error));
YouTubeDLX.Info.Extract({ query: "Soniye" })
  .on("data", data => console.log(data))
  .on("error", error => console.error(error));
YouTubeDLX.Info.Extract({ query: "Soniye" })
  .on("data", data => console.log(data))
  .on("error", error => console.error(error));
YouTubeDLX.Info.Extract({ query: "Soniye" })
  .on("data", data => console.log(data))
  .on("error", error => console.error(error));
YouTubeDLX.Info.Extract({ query: "Soniye" })
  .on("data", data => console.log(data))
  .on("error", error => console.error(error));
YouTubeDLX.Info.Extract({ query: "Soniye" })
  .on("data", data => console.log(data))
  .on("error", error => console.error(error));
YouTubeDLX.Info.Extract({ query: "Soniye" })
  .on("data", data => console.log(data))
  .on("error", error => console.error(error));
YouTubeDLX.Info.Extract({ query: "Soniye" })
  .on("data", data => console.log(data))
  .on("error", error => console.error(error));
YouTubeDLX.Info.Extract({ query: "Soniye" })
  .on("data", data => console.log(data))
  .on("error", error => console.error(error));
YouTubeDLX.Info.Extract({ query: "Soniye" })
  .on("data", data => console.log(data))
  .on("error", error => console.error(error));
YouTubeDLX.Info.Extract({ query: "Soniye" })
  .on("data", data => console.log(data))
  .on("error", error => console.error(error));
YouTubeDLX.Info.Extract({ query: "Soniye" })
  .on("data", data => console.log(data))
  .on("error", error => console.error(error));
